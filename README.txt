Instructions

1. Run command 'go run .'
2. Insert path to directory, absolute or relative, eg. "C:\security-scanner\test" or ".\test"
3. Select report format to export

After this, report will be generated with format 'SCS-dd_MM_YYYY_ss_mm_hh.<format>', <format> being the one chosen on step 3.